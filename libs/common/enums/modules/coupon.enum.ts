export enum CouponUnit {
  Money = 'Money',
  Percent = 'Percent',
}

export enum CouponType {
  Paid = 'Paid',
  Free = 'Free',
}

export enum CouponStatus {
  Active = 'Active',
  InActive = 'InActive',
  Expired = 'Expired',
}
