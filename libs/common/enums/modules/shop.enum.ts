export enum ShopStatus {
  Open = 'Open',
  Close = 'Close',
  InActive = 'InActive',
}

export enum ShopState {
  Open = 'Open',
  Close = 'Close',
}

export enum EShopRecommendStatus {
  InActive = 'InActive',
  Active = 'Active',
  Expired = 'Expired',
}
